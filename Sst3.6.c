 //WAP to print gerater between two numbers.Print number one number of both are the same.
 #include<stdio.h>
 int main()
 {
    int a,b;
    printf("Enter any two number:");
    scanf("%d %d",&a,&b);
    if(a>b)
    printf("%d is Greater Number",a);
    else if(b>a)
    printf("%d is an Greater Number",b);
    else
    printf("%d is Greater Number",a);
    return 0;

 }
